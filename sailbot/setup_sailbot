#RUN AS SOURCE#

#i.e.
# $ source setup_herons
# This will configure environment variables used for the other scripts (seen in herons_set_vars), while also adding these relevant subdirectories to your path.
# Connectivity based utilities and general utilities are separated, and each extend your path variable
# By running this alone, you will have access to all the utilities
#
_path="$HOME/pavlab_shorts/sailbot/"
for subdir in $_path* ; do
    [ -d "${subdir%/}" ] || continue
    export PATH=$PATH:"$subdir"
done


